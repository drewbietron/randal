/**
 * Context input component - text input for injecting context into running jobs.
 * Rendered inline in the dashboard HTML via JavaScript.
 */
export const CONTEXT_INPUT_COMPONENT = "context-input";
