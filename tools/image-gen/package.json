{
	"name": "@randal/image-gen-tool",
	"version": "0.1.0",
	"private": true,
	"description": "Image generation and analysis tool for Randal — generate images (OpenRouter) and analyze them with multimodal models",
	"type": "module",
	"main": "lib/index.ts",
	"scripts": {
		"typecheck": "tsc --noEmit",
		"test": "bun test"
	},
	"dependencies": {
		"@modelcontextprotocol/sdk": "^1.12.0",
		"zod": "^3.24.0"
	},
	"devDependencies": {
		"@types/bun": "^1.2.0",
		"typescript": "^5.7.0"
	}
}
